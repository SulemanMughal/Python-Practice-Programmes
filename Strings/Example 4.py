#  encode
"""Returns an encoded string. Python’s default
encoding is normally ASCII. Argument errors
defines the type of error handling used; by default,
errors is "strict"."""
print("islamabad is the captital city of pakistan".encode())